name: CI

on:
  push:
    branches: [main]
  pull_request:

# Least privilege: this workflow only reads the repository. Fork PRs get a
# read-only token, which the default scope would not give them.
permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.3.x

      - run: bun install --frozen-lockfile

      - name: Lint + format check
        run: bunx biome check .

      - name: Typecheck
        run: bunx tsc --noEmit

      - name: Test
        run: bun test

      # Catches a compile break before release.yml meets it at tag time; the
      # per-platform builds happen there.
      - name: Compile smoke
        run: |
          bun run build
          dist/agentforge --version
