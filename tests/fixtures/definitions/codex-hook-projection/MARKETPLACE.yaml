schema: agentforge.marketplace/v1
id: codex-hook-projection
defaults:
  name: codex-hook-projection
  description: Fixture marketplace for Codex hook projection compiler coverage.
packages:
  - packages/*/PACKAGE.yaml
publications:
  - id: codex
    target: codex
    destination: .agents/plugins/marketplace.json
    enrollment:
      mode: include
      packages: [guarded, notifier, triage]
