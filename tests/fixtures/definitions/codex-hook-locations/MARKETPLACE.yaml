schema: agentforge.marketplace/v1
id: codex-hook-locations
defaults:
  name: codex-hook-locations
  description: Fixture marketplace for hook-diagnostic source locations.
packages:
  - packages/*/PACKAGE.yaml
publications:
  - id: codex
    target: codex
    destination: .agents/plugins/marketplace.json
    enrollment:
      mode: include
      packages: [located, duplicated]
