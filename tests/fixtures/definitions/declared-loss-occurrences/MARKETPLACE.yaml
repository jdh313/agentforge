schema: agentforge.marketplace/v1
id: declared-loss-occurrences
defaults:
  name: declared-loss-occurrences
  description: Fixture marketplace for a declared loss matched by more than one occurrence.
packages:
  - packages/*/PACKAGE.yaml
publications:
  - id: codex
    target: codex
    destination: .agents/plugins/marketplace.json
    enrollment:
      mode: include
      packages: [twice]
