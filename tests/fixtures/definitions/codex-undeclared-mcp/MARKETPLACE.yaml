schema: agentforge.marketplace/v1
id: codex-undeclared-mcp
defaults:
  name: codex-undeclared-mcp
  description: Fixture marketplace for Codex compatibility coverage of an mcp__ reference carrying no declared loss.
packages:
  - packages/*/PACKAGE.yaml
publications:
  - id: codex
    target: codex
    destination: .agents/plugins/marketplace.json
    enrollment:
      mode: include
      packages: [mcp-skill]
