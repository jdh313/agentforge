---
name: spec-flow
description: Drive a change through the spec-flow contract lifecycle.
---

# Spec flow

Dispatch the requested lifecycle operation to the corresponding installed
`spec-flow:*` skill. Arguments: `$ARGUMENTS`
