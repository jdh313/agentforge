node_modules/
dist/
npm-stage/
.bun/
.*.bun-build
*.log
.DS_Store
/tmp/

# Local-only, never published
CLAUDE.local.md
.claude/settings.local.json
.docs/
.env
.env.*

# Agent working notes. Ignored rather than merely unused: jj snapshots new
# files automatically, so without this every agent report lands in whichever
# branch happens to be open.
.jj-scratch/
